{
  "name": "bastion",
  "version": "0.0.0",
  "private": true,
  "description": "bastiontower.com monorepo — cms (Strapi) + web (Astro) + scripts (ETL)",
  "scripts": {
    "prepare": "lefthook install"
  },
  "devDependencies": {
    "lefthook": "^1.10.0"
  }
}
